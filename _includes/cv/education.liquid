{% comment %}
  Unified education entry renderer for RenderCV and JSON Resume. Empty dates
  and locations do not reserve a metadata column.
{% endcomment %}

<ul class="card-text font-weight-light list-group list-group-flush">
  {% for entry in entries %}
    {% assign start_date = entry.start_date | default: entry.startDate %}
    {% assign end_date = entry.end_date | default: entry.endDate %}
    {% assign date = entry.date %}
    {% if date == blank and entry.startDate != blank %}
      {% assign jsonresume_end_date = entry.endDate | default: 'Present' %}
      {% assign date = entry.startDate | append: ' - ' | append: jsonresume_end_date %}
    {% elsif date == blank and entry.endDate != blank %}
      {% assign date = entry.endDate %}
    {% elsif date == blank and start_date != blank %}
      {% assign start_year = start_date | split: '-' | first %}
      {% assign end_year = end_date | split: '-' | first | default: 'Present' %}
      {% assign date = start_year | append: ' - ' | append: end_year %}
    {% elsif date == blank and end_date != blank %}
      {% assign date = end_date %}
    {% endif %}
    {% assign location = entry.location | default: '' | strip %}
    {% assign has_metadata = false %}
    {% if date != blank or location != blank %}{% assign has_metadata = true %}{% endif %}
    {% assign study_type = entry.degree | default: entry.studyType %}
    {% assign entry_title = study_type | default: entry.area | default: entry.institution %}
    {% assign entry_url = entry.url | default: '' | strip %}
    {% assign entry_url_scheme_7 = entry_url | slice: 0, 7 | downcase %}
    {% assign entry_url_scheme_8 = entry_url | slice: 0, 8 | downcase %}
    {% unless entry_url_scheme_7 == 'http://' or entry_url_scheme_8 == 'https://' %}
      {% assign entry_url = '' %}
    {% endunless %}

    <li class="list-group-item">
      <div class="row">
        {% if has_metadata %}
          <div class="col-xs-2 col-sm-2 col-md-2 text-center date-column cv-entry-meta">
            <table class="table-cv">
              <tbody>
                {% if date != blank %}
                  <tr>
                    <td>
                      <span class="badge font-weight-bold danger-color-dark text-uppercase align-middle" style="min-width: 75px">{{ date }}</span>
                    </td>
                  </tr>
                {% endif %}
                {% if location != blank %}
                  <tr>
                    <td>
                      <p class="location">
                        <i class="fa-solid fa-location-dot iconlocation" aria-hidden="true"></i>
                        {{ location }}
                      </p>
                    </td>
                  </tr>
                {% endif %}
              </tbody>
            </table>
          </div>
          <div class="col-xs-10 col-sm-10 col-md-10 cv-entry-content">
        {% else %}
          <div class="col-xs-12 col-sm-12 col-md-12 cv-entry-content">
        {% endif %}
          {% if entry_title != blank %}
            <h3 class="title font-weight-bold ml-1 ml-md-4" data-toc-skip>
              {% if entry_url != blank %}<a href="{{ entry_url | escape }}">{{ entry_title }}</a>{% else %}{{ entry_title }}{% endif %}
            </h3>
          {% endif %}

          {% if entry.institution != blank and entry.institution != entry_title %}
            <p class="cv-entry-institution ml-1 ml-md-4 mb-1" style="font-size: 0.95rem">{{ entry.institution }}</p>
          {% endif %}

          {% if entry.area != blank and entry.area != entry_title %}
            <p class="cv-entry-area ml-1 ml-md-4 mb-1" style="font-size: 0.95rem; font-style: italic">{{ entry.area }}</p>
          {% endif %}

          {% if entry.score != blank %}
            <p class="cv-entry-score ml-1 ml-md-4 mb-1" style="font-size: 0.95rem"><strong>Score:</strong> {{ entry.score }}</p>
          {% endif %}

          {% if entry.summary != blank %}
            <div class="cv-entry-summary cv-markdown-block ml-1 ml-md-4 mb-1" style="font-size: 0.95rem; font-style: italic">
              {{ entry.summary | markdownify }}
            </div>
          {% endif %}

          {% if entry.courses != blank or entry.highlights != blank %}
            <ul class="items">
              {% for item in entry.courses %}
                <li><span class="item">{{ item }}</span></li>
              {% endfor %}
              {% for item in entry.highlights %}
                <li><div class="item cv-markdown-block">{{ item | markdownify }}</div></li>
              {% endfor %}
            </ul>
          {% endif %}
        </div>
      </div>
    </li>
  {% endfor %}
</ul>
